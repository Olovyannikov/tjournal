import { FullPost, PostComments } from '@/components';

export const NewsItemsPage = (): JSX.Element => {
    return (
        <>
            <FullPost />
            <PostComments />
        </>
    );
};
