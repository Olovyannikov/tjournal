import { Post } from '@/components';

export const IndexPage = (): JSX.Element => {
    return (
        <section>
            <Post />
            <Post />
            <Post />
        </section>
    );
};
