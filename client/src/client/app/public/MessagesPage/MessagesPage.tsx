import { MainLayout } from '@/layouts';

export const MessagesPage = ({}): JSX.Element => {
    return (
        <MainLayout>
            <section>
                <h1>Messages</h1>
            </section>
        </MainLayout>
    );
};
