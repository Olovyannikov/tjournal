import { MainLayout } from '@/layouts';

export const FollowsPage = ({}): JSX.Element => {
    return (
        <MainLayout>
            <section>
                <h1>FollowsPage</h1>
            </section>
        </MainLayout>
    );
};
